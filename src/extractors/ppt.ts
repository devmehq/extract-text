/* eslint-disable */

import { CallbackType } from '../util';
//, ppt = require( 'ppt' );

// todo
export const extract = function (filePath: string, options: any, cb: CallbackType) {
  /*
  var captured = ppt.readFile(filePath);
  console.log('CAPTURED!!!!')
  console.log(captured)
  console.log('CAPTURED!!!!')
  cb( null, null );
    if ( error ) {
      cb( error, null );
      return;
    }
    cb( null, data.toString() );
  */
};

export const types = ['application/vnd.ms-powerpoint'];
